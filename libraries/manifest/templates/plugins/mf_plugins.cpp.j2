#include <appbase/application.hpp>
#include <boost/preprocessor/seq/for_each.hpp>

#include <morphene/manifest/plugins.hpp>

{% for plugin in plugins %}
#include <morphene/plugins/{{ plugin["plugin_name"] }}/{{ plugin["plugin_name"] }}_plugin.hpp>
{% endfor %}

namespace morphene { namespace plugins {

void register_plugins()
{
   {% for plugin in plugins %}
   appbase::app().register_plugin< morphene::plugins::{{ plugin["plugin_namespace"] }}::{{ plugin["plugin_name" ] }}_plugin >();
   {% endfor %}
}

} }
